Manual or ad hoc test scripts that were previously loose in the project root.

These are separate from the main automated suites under `tests/` and should be
run intentionally rather than assumed to be part of the default test matrix.
