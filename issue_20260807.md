# Issue Log — DHIS2 dataset lifecycle (new datasets disappear / break)

> Working troubleshooting log. Started 2026-08-07. Append dated entries under
> **Troubleshooting log** as we proceed. Git-ignored / local doc (like `guide.md`).

---

## 1. Problem statement

Newly created DHIS2 datasets (e.g. `mal_pregnancy`, `mal_prevention`) do not
survive: they **disappear from the Datasets list**, and/or their map shows
**"Empty query?"**, the **data filter is empty**, and editing throws
**"Failed to load dataset configuration for editing."** Healthy long-standing
datasets (the `MAL - …` originals) are unaffected. The failures cluster on
**recreated / re-registered** datasets.

### Symptoms seen (all trace to a small set of root causes)
| Symptom | Root cause # |
|---|---|
| Dataset vanishes from list after restart / re-create | 1, 2 |
| Map: **"Empty query?"** | 1 (→ no columns) |
| Data filter: no values | 1, 8 |
| Edit: **"Failed to load dataset configuration for editing"** | 1 (staged dataset gone) |
| Wrapper query → **404** | 3 |
| Indicator map shows summed % (e.g. 2,877%) | 4, 5 |
| Period slider dead "at times" | 6 |
| Sync returns 0 rows (indicators) | 7 |
| Sync returns ~481K rows / wrong level | 4 |

---

## 2. Root causes & what's been done

Legend for **Status**: ✅ committed + deployed · 🟡 committed, **NOT deployed** · ⚪ not started / config-only.

### #1 — cleanup cascade orphans the staged dataset  ← **primary root cause**
A DHIS2 staged dataset has **three** linked SqlaTables (SOURCE, MART, and the
user-facing METADATA wrapper). On SqlaTable `after_delete` the code ran
`DELETE FROM dhis2_staged_datasets` with **no check that the other tables still
referenced it**. Re-registering a wrapper deletes+recreates it → that delete
nuked the whole staged dataset → SOURCE/MART + the new wrapper are **orphaned**
(no columns, no staged config).

**Two `after_delete` handlers do this cleanup — BOTH had to be guarded:**
- `SqlaTable.cleanup_linked_dhis2_staged_dataset` (`connectors/sqla/models.py`).
- `_after_sqla_table_delete` (`dhis2/listeners.py`) — a **separate** listener
  registered via `setup_listeners`, only skipping MARTs.

Guarding just the first was **not enough** — the second listener overrode it and
still deleted the staged dataset (proven live; see the BREAKTHROUGH log entry).
- **Fix:** the same "is this the last linked table?" guard applied to **both**
  handlers. +tests in `models_test.py` and `test_cascading_delete.py` (4/4 pass).
- **Status:** models.py guard ✅ deployed (`a60f614853`, `grep remaining_links`
  = 3 on server). listeners.py guard 🟡 committed locally, **DEPLOY NEXT** —
  this is the one that actually stops the disappearing.

### #2 — startup compat backfill churns / deletes wrappers
`run_compatibility_backfill()` ran on **every** app init and re-registered /
de-duplicated DHIS2 wrappers; with duplicate datasets present its cascade
deleted live ones. Restart = dataset roulette.
- **Fix:** `DHIS2_DISABLE_STARTUP_BACKFILL` flag guards it in
  `superset/initialization/__init__.py`.
- **Status:** ✅ deployed (commit `67c2249a0c`) + flag set in `/etc/superset/superset.env`.
  Log shows `DHIS2 compat backfill skipped`.

### #3 — wrapper SQL built unquoted → routed to the DHIS2 API → 404
Wrapper SQL was `f"SELECT * FROM {serving_table_ref}"` (raw interpolation).
Unquoted `dhis2_serving.sv_N` → the DHIS2 dialect parses `dhis2_serving` as an
API endpoint → `https://hmis.health.go.ug/api/dhis2_serving` → 404.
- **Fix:** `_build_metadata_wrapper_sql()` always backtick-quotes via
  `_parse_table_ref`. +tests. Plus a one-time `repair_wrapper_sql.py` for
  existing rows.
- **Status:** ✅ deployed (commit `781445181b`). Repair script available.

### #4 — `org_unit_scope="all_levels"` (volume + indicator correctness)
Recovery/repair paths hard-code `all_levels` + a root org unit → the resolver
enumerates the whole hierarchy (levels 1–7) → ~481K rows, and for **indicators**
the map SUMs facility percentages into districts (2,877%). Scoping too high
also drops the district level entirely.
- **Mitigation:** cap `max_orgunit_level=3` / use district-level `selected`
  scope when creating.
- **Status:** ⚪ config-level only. Code change (don't force `all_levels`,
  level-aware maps) not done — larger/riskier.

### #5 — indicators re-aggregated (SUM) instead of NONE
The non-re-aggregate guard checked only `dhis2_is_indicator`; when that marker
drifted, indicators fell through to `SUM` → summed percentages.
- **Fix:** resilient detection — also honour `dhis2_variable_type` in
  `dataset_columns_payload` (`analytical_serving.py`). +tests.
- **Status:** ✅ deployed (commit `fc5d73bd0f`).

### #6 — period slider grabs the wrong / blank column
The map resolved the period column from loaded data-row keys + a name heuristic
that matched `period_variant` (often blank) → slider dead "at times."
- **Fix:** marker-driven `resolveDhis2PeriodColumn` (prefers
  `dhis2_period_key='period'`). +tests.
- **Status:** ✅ deployed (commit `7fbc43d6b5`).

### #7 — indicator analytics time out (0 rows)
50-OU chunks for indicators (heavy per-cell formula + OU count map) exceed the
Cloudflare ~100 s gateway timeout → 5xx → whole sync aborts → 0 rows.
- **Fix:** indicator-aware OU chunking (indicators at 10 OUs, data elements at
  50) + 525 added to retryable codes (`sync_service.py`). +tests.
- **Status:** ✅ deployed (commit `6ff1022e05`).

### #8 — orphan wrapper has no columns (defense-in-depth)
When the staged dataset is gone, column sync has no manifest → wrapper has no
columns → "Empty query" + empty filter. #1 prevents the orphaning; this would
make even an existing orphan self-heal.
- **Fix (proposed):** column sync falls back to introspecting the physical
  serving table when the staged dataset is absent.
- **Status:** ⚪ not started.

### #9 — data filter needs a staged link
`DHIS2ColumnFilterControl` fetches column values from the staged-dataset
endpoint; static/orphan datasets have no staged link → empty filter.
- **Fix (built earlier):** `/dhis2/staged-datasets/dataset-column-values`
  read endpoint + frontend fallback by `dataset_id`.
- **Status:** ⚪ uncommitted (from an earlier session).

### #10 — chart metadata loses its durable DHIS2 identity on dataset re-registration
When a DHIS2 dataset is deleted and re-created, the chart keeps the old
Superset `SqlaTable.id` in serialized JSON (`params.datasource`,
`query_context.datasource.id`, and related payloads). Superset nulls
`slices.datasource_id`, but the JSON blob is not rewritten automatically.
That makes the chart look orphaned even though the underlying DHIS2 dataset
has been re-registered.

- **Fix (implemented locally in the current patch):** persist the logical
  `dhis2_staged_dataset_id` in DHIS2 dataset metadata, verify the metadata
  `SqlaTable` registration after rebuild, and repair chart references during
  the registration flow so re-created datasets can reclaim charts instead of
  looking orphaned.
- **Related guardrails:** the read path now avoids forcing a rebuild when the
  serving table already exists, and the code keeps the old test seams by
  exposing module-level wrappers for registration / repair helpers.
- **Status:** 🟡 committed locally, validated with targeted tests.
- **Impact:** this closes the gap for dataset persistence across UI create /
  recreate / re-register flows, and gives a clear regression target if a
  future metadata cleanup accidentally breaks reattachment again.

---

## 3. Deploy status at a glance
| Fix | Commit | Live? |
|---|---|---|
| #7 indicator chunking | `6ff1022e05` | ✅ |
| #5 indicators no re-aggregate (+ map PNG, CMS) | `fc5d73bd0f` | ✅ |
| #6 period column resolver | `7fbc43d6b5` | ✅ |
| #2 backfill guard | `67c2249a0c` + env flag | ✅ |
| #3 wrapper SQL quoting | `781445181b` | ✅ |
| #1 cleanup-cascade guard — models.py | `a60f614853` | ✅ |
| #1 cleanup-cascade guard — listeners.py | deployed | ✅ **CONFIRMED — survives restart** |
| #8 resilient column sync | — | ⚪ |
| #9 filter fallback endpoint | — | ⚪ |
| #10 durable chart identity | — | ⚪ |

**Confirmed cause of the recurrence:** the models.py guard was deployed but a
**second unguarded `after_delete` listener** (`listeners.py`) overrode it and
kept deleting the staged dataset. That listener is now guarded (committed
locally) — **deploy it, then re-test a clean create.** This is the last piece.

---

## 4. Handy server commands (reference)

**Enter the DHIS2 superset shell (env loaded — avoids the empty-SQLite trap):**
```bash
cd /opt/dhis2-superset && sudo -u superset env HOME=/tmp bash -lc 'set -a; . /etc/superset/superset.env; set +a; /opt/superset-venv/bin/superset shell'
```

**Run a `/tmp` script non-interactively** (single `exec` line via heredoc — avoids
the multi-line REPL `IndentationError`):
```bash
cd /opt/dhis2-superset && sudo -u superset env HOME=/tmp bash -lc 'set -a; . /etc/superset/superset.env; set +a; /opt/superset-venv/bin/superset shell' <<'SH'
exec(open('/tmp/find_orphans.py').read())
SH
```

**Check whether a fix is deployed** (example: the #1 cleanup guard):
```bash
grep -c "remaining_links" /opt/dhis2-superset/superset/connectors/sqla/models.py   # 0 = not deployed
```

**Deploy a single backend file (no npm build):**
```bash
cd /opt/dhis2-superset
git fetch https://github.com/Ex057/Martbase.git martbasev1
git checkout FETCH_HEAD -- <path/to/file.py>
sudo systemctl restart superset superset-worker superset-beat
```

---

## 5. Troubleshooting log

### 2026-08-07 — new dataset disappeared again
- Observation: a freshly created dataset vanished from the list.
- Hypothesis: #1 (cleanup cascade) still live on the server — the guard fix is
  committed locally but **not deployed**.
- Next step: deploy #1, verify, then create a clean dataset and confirm it
  survives a wrapper re-register + a restart.

**Findings (evening):**
- `grep -c remaining_links models.py` = **3** → **#1 guard IS now deployed.** ✅
- Orphan scan (`/tmp/find_orphans.py`): existing staged ids `[1-9, 28]`;
  **staged 29 (mal_pregnancy) GONE**, wrapper 283 GONE. Orphans = **source 279
  + mart 281** (`sv_29`, ~1,903 rows still in ClickHouse) pointing at staged 29.
- Interpretation: this is **pre-guard damage** — staged 29 was deleted before
  the guard went live. The guard prevents recurrence but cannot resurrect it.
  `mal_pregnancy` is invisible because staged dataset + wrapper are gone; only
  the raw serving tables remain (not shown in the METADATA-only list).
- **Open question:** was mal_pregnancy created *before* or *after* the guard
  deploy? Decides whether the guard is sufficient or a 2nd deletion path exists.
- **Next:** (a) delete the 2 orphans (279, 281) — safe now, staged 29 already
  gone so no further cascade; (b) recreate mal_pregnancy at **district level**
  with the guard live; (c) trigger a wrapper re-register (or restart) and
  confirm the staged dataset + wrapper **survive**. Log the result.
- If it survives → #1 confirmed sufficient. If it disappears again → hunt the
  second path (candidates: `create_staged_dataset` name-collision upsert;
  `register_metadata_dataset_as_superset_dataset` stale-record deletion).

**Findings (create test — REFRAME):**
- Created a new `mal_pregnancy_dataset` with the guard live; it "disappeared".
- `/tmp/post_create.py` shows: staged ids still `[1-9, 28]` (**no new staged**),
  newest SqlaTables are the **old** `sv_29` orphans 279/281 (**no new serving
  tables, no sv_30**), and **no new sync job** (newest is 3864 for staged 8).
- **New diagnosis:** this is NOT the cascade-delete (#1). The create **never
  persisted** — it **rolled back**. `create_staged_dataset` rolls back the whole
  ORM txn on any error; nothing lands (no staged, no serving, no job).
- **Prime suspect:** the leftover **orphan `sv_29` tables (279/281)** poison new
  creates — `register_serving_table_as_superset_dataset` matches existing
  SqlaTables by serving-table-ref / staged id and hits the orphans pointing at
  the dead staged 29 → error → rollback.
- **Next:** (a) capture the create error from `journalctl`; (b) delete orphans
  279/281; (c) retry create and confirm a new staged id + sync job appear.
- Note: #1 (cleanup guard) is still correct and worth keeping — it fixes the
  *cascade* class — but it is NOT the cause of *this* (create-rollback) failure.
  So there are (at least) TWO distinct failure classes:
  **(1) cascade-delete after create**, and **(2) create rolls back / never
  persists**, the latter likely triggered by leftover orphans.

**BREAKTHROUGH — the actual recurring root cause:**
- Deleting orphan 279 logged BOTH handlers firing back-to-back:
  1. `cleanup_linked_dhis2_staged_dataset: 1 other SqlaTable still references
     staged 29 — keeping it` → the **models.py guard worked**. ✅
  2. `DHIS2 listener: SqlaTable id=279 deleted; cleaning up DHIS2StagedDataset
     id=29` → a **SECOND, separate `after_delete` handler** in
     `superset/dhis2/listeners.py::_after_sqla_table_delete` that deletes the
     staged dataset with **NO guard** (it only skips MARTs). It **overrode** the
     models.py guard.
- So the "create rolls back" idea was a red herring. Real mechanism: the
  **unguarded listener** deletes the staged dataset whenever a SOURCE/wrapper
  SqlaTable is deleted — which happens on every wrapper **re-registration**.
  That's why datasets kept vanishing even with the models.py guard deployed.
- **Fix:** applied the identical "other tables still link?" guard to
  `_after_sqla_table_delete`. Both `after_delete` handlers now agree. Also
  repaired 2 stale tests in `test_cascading_delete.py`; **4/4 pass**.
- **Status:** models.py guard = deployed (`a60f614853`). **listeners.py guard =
  committed locally, DEPLOY NEXT** — this is the one that actually stops it.
- **Debt:** two overlapping `after_delete` handlers do the same cleanup;
  guarding one wasn't enough. Consolidate to a single guarded path later.

### 2026-08-19 — chart persistence investigation
- Observed broken charts with `slices.datasource_id = NULL` and stale JSON
  refs like `306__table` / `327__table` in `params.datasource` and
  `query_context.datasource.id`.
- The existing cleanup path deletes stale `SqlaTable` rows, but it does not
  rewrite chart JSON or preserve a logical dataset id for future rebinds.
- Conclusion: dataset survival is fixed, but chart persistence still needs a
  durable logical-key fix so re-created datasets can reclaim charts instead of
  leaving them orphaned.

### 2026-08-19 — dataset persistence / rebind hardening
- Confirmed the new DHIS2 create/register path now keeps the dataset visible
  after UI creation by verifying the metadata registration instead of
  assuming the insert succeeded.
- `ensure_serving_table()` now:
  - registers the serving, MART, and metadata datasets through module-level
    wrappers so tests can patch the seams reliably,
  - treats `serving_table_exists(...)` as a strict boolean check so a mocked
    engine does not accidentally take the DB-backed branch,
  - keeps chart-repair failures non-fatal, but still raises on an explicit
    metadata verification failure.
- Validation:
  - `pytest tests/dhis2/test_staged_dataset_service.py tests/unit_tests/dhis2/test_superset_dataset_service.py tests/unit_tests/dhis2/test_backfill.py -q`
  - result: `43 passed`
- Use this entry as the reference when a future regression looks like
  "dataset created in UI, then disappears / is inaccessible / cannot be
  re-bound after re-registration" even though the serving tables still exist.

### 2026-08-20 — chart-ref repair for stale columns after rebind
- New failure class observed after the Aug 19 rebind work:
  charts could be re-pointed to the live datasource and still fail at query
  time with `Columns missing in dataset: [...]` because the saved query payload
  still held stale column refs.
- The repair path now rewrites chart query content in addition to datasource
  ids:
  - `params`
  - `query_context.form_data`
  - `query_context.queries[*]`
  - common column-like fields such as `columns`, `groupby`, `filters`, and
    `orderby`
- The rewrite is conservative: it only remaps stale refs when the live
  datasource exposes one unambiguous replacement, using stable DHIS2 metadata
  such as column names, `verbose_name`, and `dhis2_variable_id`.
- Added a server-side repair helper:
  `scripts/local/repair_dhis2_chart_bindings.py`
  so the chart repair can be run in `superset shell` against a staged dataset
  or as a full backfill.
- Validation:
  - `pytest tests/unit_tests/dhis2/test_superset_dataset_service.py tests/unit_tests/dhis2/test_backfill.py -q`
  - result: `14 passed`
- This is the fix to use when a chart is reattached to the correct datasource
  but still trips Superset validation because old column names survived in the
  serialized query state.

### 2026-08-20 — overnight persistence hardening for chart bindings
- Implemented a second-stage hardening pass for staged DHIS2 dataset refreshes
  so nightly registration does not rebind charts through `SOURCE`, `MART`, and
  `METADATA` in sequence.
- The routine refresh path now runs one role-preserving chart repair after all
  dataset registrations complete:
  - preserve an existing eligible `MART` binding
  - preserve an existing eligible `METADATA` binding
  - repair serialized chart payload metadata in place
  - rebind only invalid / drifted `DHIS2_SOURCE_DATASET` charts
  - prefer `MART`, fall back to `METADATA`, never target `SOURCE`
- Startup/backfill repair was aligned to the same rule so compatibility
  backfill does not undo the nightly fix by rebinding charts to `SOURCE`.
- Validation:
  - `pytest -q tests/unit_tests/dhis2/test_superset_dataset_service.py`
  - result: `18 passed`
- Intended effect:
  staged datasets should keep stable `SqlaTable` identities and charts should
  remain attached to an eligible datasource role after the overnight sync path
  completes.

### 2026-08-20 — planned overnight verification for `test_03_dataset`
- I will create a staged dataset named `test_03_dataset` and use it as the
  next live overnight persistence check.
- A chart named `test_03` under `test_03_dataset` has also been created and is
  part of the overnight verification target.
- Verification date:
  **Friday, August 21, 2026**.
- What to confirm tomorrow:
  - the staged dataset row still exists
  - the expected `SOURCE`, `MART`, and `METADATA` SqlaTable rows still exist
  - the chart-facing datasource remains bound to `MART` or `METADATA`
  - no chart or dataset drifted back to `DHIS2_SOURCE_DATASET`
  - the dataset is still visible / usable from the UI after the overnight run
- Read-only server confirmation helper:
  `scripts/local/confirm_test_03_overnight.py`
- Suggested server usage:
```bash
cp /opt/dhis2-superset/scripts/local/confirm_test_03_overnight.py /tmp/
cd /opt/dhis2-superset && sudo -u superset env HOME=/tmp bash -lc 'set -a; . /etc/superset/superset.env; set +a; /opt/superset-venv/bin/superset shell' <<'SH'
exec(open('/tmp/confirm_test_03_overnight.py').read())
SH
```

### 2026-08-07 — ✅ CONFIRMED FIXED (listeners.py guard deployed)
- Deployed the `listeners.py` guard + restarted.
- Created a fresh `mal_pregnancy_dataset`, synced to `success`.
- `/tmp/post_create.py` **before AND after a restart** = identical:
  `staged 31 | mal_pregnancy_dataset | status=success | wrapper=292`, with
  SqlaTables 292 (wrapper) / 290 (mart) / 288 (source) all present.
- **The dataset survives a restart** — the disappearing bug is resolved.
- Scope also correct: mart = **1,618 rows** (~130 districts × 12 months, i.e.
  district level), source = 22,448 raw pre-pivot rows.
- Both `after_delete` handlers (models.py + listeners.py) are now guarded and
  deployed; no third handler exists.
- **Remaining (optional, defense-in-depth):** #8 resilient column sync, #9
  filter fallback endpoint, and consolidating the two `after_delete` handlers.

### 2026-08-07 — ✅ scheduled-sync path also confirmed safe
- Worry: dataset was created+fine one day, gone the next → suspected the
  **overnight scheduled sync** (celery-beat). Yesterday's loss = the unguarded
  listener firing during that sync's re-registration (pre-fix).
- Test: ran `schedule_staged_dataset_sync(31, job_type="scheduled",
  incremental=True)` — the exact nightly code path — **3×** (jobs 3866/3867/3868,
  all `success`, 1,618 rows each).
- Result: `staged 31 mal_pregnancy_dataset` + wrapper 292 + source/mart all
  **survived every run.** The scheduled sync updates in place and only cleans up
  MARTs (listener skips) / legacy datasets (none present); the guard covers the
  rest. **Overnight risk resolved.**
- Monitoring: user watching. Quick daily check one-liner is in §4-ish below.
- If it EVER recurs: run `/tmp/find_orphans.py` + grep the worker log for
  `cleaning up DHIS2StagedDataset` **without** a following `keeping it` — that
  would indicate a THIRD deletion path; log it here.

### 2026-08-10 — recurrence triage kit prepared
- Added read-only helper:
  `scripts/local/diagnose_dhis2_dataset_disappearance.py`
- Server usage:
```bash
cp /opt/dhis2-superset/scripts/local/diagnose_dhis2_dataset_disappearance.py /tmp/
cd /opt/dhis2-superset && sudo -u superset env HOME=/tmp DHIS2_DIAG_TARGET=mal_pregnancy bash -lc 'set -a; . /etc/superset/superset.env; set +a; /opt/superset-venv/bin/superset shell' <<'SH'
exec(open('/tmp/diagnose_dhis2_dataset_disappearance.py').read())
SH
```
- The helper is intentionally **read-only**. It prints:
  matching staged datasets, matching SqlaTables, orphaned SqlaTables, recent
  sync jobs, and the exact `journalctl` command to run next.
- Use it first to classify the recurrence before deleting/recreating anything:
  `staged row deleted`, `metadata row missing`, `orphaned SqlaTables`, or
  `rows intact but query/listing broken`.

### 2026-08-12 — current prod state + pause point
- Server confirmed on startup:
  `DHIS2 compat backfill skipped (DHIS2_DISABLE_STARTUP_BACKFILL set)`.
- Read-only diagnostic script executed successfully from `/tmp` via:
  `exec(open('/tmp/diagnose_dhis2_dataset_disappearance.py').read(), {"__name__": "__main__"})`.
- Diagnostic result for `mal_pregnancy`:
  **no matching staged dataset, no matching SqlaTable, no orphaned SqlaTable**.
- Interpretation: this is **not** the earlier partial-orphan state. As of
  **Wednesday, August 12, 2026**, `mal_pregnancy` has no remaining metadata
  trace in the current metadata DB.
- Separate finding: `journalctl` showed fresh `ensure_serving_table` rebuild
  activity/errors for dataset ids **1** (`test map`) and **2**
  (`IRS Compaigns`) around **2026-08-12 15:17–15:18**. That looks like a
  distinct current issue affecting surviving datasets, not direct evidence of
  the old `mal_pregnancy` disappearance path.
- Current staged datasets snapshot from prod shell:
  newest rows include `28 mal_prevention`, then `9..1`; there is **no**
  `mal_pregnancy` / `mal_pregnancy_dataset` row present now.
- Operational note: multi-line pastes into `superset shell` and shell heredocs
  repeatedly caused **indentation / heredoc delimiter errors** during
  troubleshooting. Going forward, create Python helpers as files in `/tmp` and
  execute them from the shell rather than pasting multi-line blocks directly.
- Decision / pause point:
  recreate the dataset as **`mal_preg_dataset`**, build the charts, then check
  again tomorrow to see whether it persists overnight.

### 2026-08-13 — live log pass + current fix hypothesis
- Ran `./deploy-malaria-update.sh logs` on prod and got a useful live window
  around **2026-08-13 08:42**.
- The logs show `datasource=299__table` still serving chart requests at
  **08:42:01–08:42:04**, so the `mal_preg_dataset` family was still active at
  that point.
- At **08:42:23**, Superset tried to resolve the legacy DHIS2 instance for
  `database_id=14` and failed repeatedly with:
  `no DHIS2 host found in encrypted_extra for database_id=14 – skipping`.
- The same timestamp also logged:
  `No Celery workers available for database id=14, using thread fallback`.
- Interpretation: the worker pool being unavailable is **relevant**, but this
  log does **not** prove that worker loss alone deleted the dataset. Superset
  still continued via thread fallback, so the harder failure is the broken
  `encrypted_extra` / missing DHIS2 host metadata on `database_id=14`.
- Current best fix candidate:
  1. Re-save Database 14 in the UI so `encrypted_extra` gets repopulated.
  2. Verify Celery / beat / worker are actually up when the next sync or
     metadata refresh runs.
  3. Re-test `mal_preg_dataset` and watch whether `299__table` still resolves.
- This log pass tightens the failure window, but the dataset-removal mechanism
  still looks more like metadata resolution failure than physical deletion.

### 2026-08-13 — cleanup sweep negative, credentials now the lead suspect
- Ran broad log sweeps for:
  - `DELETE /api/v1/dhis2/staged-datasets/32`
  - `delete_staged_dataset`
  - `Deleted DHIS2StagedDataset`
  - `cleanup_orphans`
  - `cleanup-stale-datasets`
  - `cleanup_linked_dhis2_staged_dataset`
- Result: no dataset delete / cleanup event was found in the current log window.
- The earlier broad grep returning many unrelated lines was a shell pattern issue,
  not evidence of deletion activity.
- The best supported failure chain now points to Database 14 metadata:
  - at **08:42:23** Superset tried to resolve the legacy DHIS2 instance for
    `database_id=14`
  - the lookup failed with:
    `no DHIS2 host found in encrypted_extra for database_id=14 – skipping`
  - Superset also logged:
    `No Celery workers available for database id=14, using thread fallback`
- Interpretation: worker availability may have been degraded, but the concrete
  breakage is the missing DHIS2 host credentials in `Database.encrypted_extra`.
  That is now the first thing to repair before chasing deletion paths further.
- Next action:
  re-save Database 14 in the UI and verify the encrypted payload is restored,
  then re-test whether `299__table` and the staged dataset reappear normally.

### 2026-08-13 — Database 14 confirmed empty
- Ran a direct Superset shell check against `Database(id=14)`.
- Result:
  - `encrypted_extra: {}`
  - `keys: []`
  - `instances: []`
- Interpretation:
  - the legacy DHIS2 metadata for Database 14 is completely absent
  - `migrate_legacy_instance(database_id=14)` has nothing to migrate because
    there is no `host` / `url` payload in `encrypted_extra`
  - this strongly supports the metadata-loss / configuration-loss theory over
    any dataset deletion theory
- Immediate fix path:
  1. Re-save Database 14 in the Superset UI and restore the DHIS2 host/URL.
  2. Confirm at least one `DHIS2Instance` row exists afterward.
  3. Re-run the dataset flow and check whether `299__table` is recreated and
     visible again.

### 2026-08-13 — DB5 vs DB14 comparison suggests a UI fallback, not data movement
- Compared a healthy database (`Database id=5`, `GF MOH Malaria Repository`)
  with `Database id=14`.
- Result:
  - DB 14: empty `encrypted_extra`, no instances
  - DB 5: empty `encrypted_extra`, but two live `DHIS2Instance` rows
- This means the working case is **not** stored in `encrypted_extra`; its live
  DHIS2 config lives in `dhis2_instances`.
- The likely place where the UI can "switch" databases is the DHIS2 database
  picker:
  - the hook in `useDHIS2Databases.ts` reads `?database=` from the URL
  - if that value is missing or no longer matches the loaded list, it falls
    back to `databases[0]`
- That makes the database context a **frontend selection problem**, not a
  relocation of dataset metadata from DB 5 to DB 14.
- The remaining thing to verify is the order returned by `/api/v1/database/`
  at runtime. If it comes back alphabetically, DB 14 can become the default
  selection simply because `"DHIS2"` sorts before `"GF MOH Malaria Repository"`.

### 2026-08-13 — ClickHouse is registered, but intentionally hidden
- Traced the ClickHouse serving-database path in `clickhouse_engine.py`.
- Result:
  - `get_or_create_superset_database()` creates or updates a real Superset
    `Database` row for the ClickHouse serving engine.
  - That row is marked `dhis2_staging_internal=True` /
    `is_dhis2_staging_internal=True`.
  - The general database list applies `DatabaseDHIS2StagingInternalFilter`,
    which explicitly excludes internal DHIS2 staging/serving databases.
- Interpretation:
  - ClickHouse is not disappearing because it was deleted.
  - It is present but hidden from the ordinary picker by design.
  - The actual user-facing bug is the frontend context fallback: if a requested
    dataset cannot be resolved, the page must not silently promote another
    dataset into view.

### 2026-08-13 11:38 EAT — prep for recreate test
- Current time on the server-side troubleshooting log checkpoint: **2026-08-13
  11:38 EAT**.
- Next test plan:
  - recreate `mal_preg_dataset` tomorrow
  - verify it stays attached to the GF DHIS2 database
  - verify the bound `DHIS2Instance` rows are present after save
  - confirm the page does not fall back to another database when opened via
    `?dataset=...`
- If the dataset comes back without instance links, the first thing to inspect
  is whether the dataset save preserved `dataset_config.variable_mappings` and
  `configured_connection_ids`.

### 2026-08-14 — dataset removed again; likely delete cascade, not instance loss
- Verified on the server that `mal_preg_dataset` is gone:
  - `DHIS2StagedDataset` match: `[]`
  - `DHIS2DatasetVariable` match: `[]`
  - `SqlaTable` match: `[]`
  - GF database instances still present:
    - `Routine eHMIS instance`
    - `Non-Routine eHMIS`
- Follow-up grep identified the removal paths worth auditing:
  - `superset/connectors/sqla/models.py`
    - `SqlaTable.after_delete()`
    - `cleanup_linked_dhis2_staged_dataset()`
  - `superset/dhis2/staged_dataset_service.py`
    - `delete_staged_dataset()`
  - `superset/dhis2/staged_dataset_api.py`
    - `cleanup_orphans()`
- Current interpretation:
  - this is not ClickHouse ownership
  - this is not instance credential loss
  - the dataset is being removed from metadata entirely, most likely through a
    delete cascade or orphan cleanup path
- Next fix target:
  - make sure deleting one linked `SqlaTable` cannot remove a live staged
    dataset that still has valid metadata/instance links
  - add a regression test for the `mal_preg_dataset` deletion path so the
    cleanup does not silently remove the staged dataset again

### 2026-08-14 — follow-up validation plan
- I will commit and push the fix, then re-test by creating `mal_preg_dataset`
  again and also creating `test_dataset`.
- The validation goal is to confirm both datasets survive normal save/update
  churn and do not disappear through delete cascade or orphan cleanup paths.

### 2026-08-14 — current state confirmed
- As of now, both `mal_preg_dataset` and `test_dataset` are done as at 11:22 am 14th august 2026
- Both datasets have charts.
- Both datasets are on a dashboard.
- Next test pass is tomorrow, to confirm they still persist after the next
  round of saves and lifecycle events.


What to expect:

  - The owning DHIS2 database stays the GF database.
  - ClickHouse is the serving/internal database used for local staging/serving tables, not the dataset’s owner. The ClickHouse DB row is intentionally hidden from the normal picker.
  - The local-data page now resolves ?dataset=... to its owning database_id first, so it should not silently jump to the first database just because the URL is missing database=. See superset-frontend/src/
    features/dhis2/useDHIS2Databases.ts:68 and superset-frontend/src/pages/DHIS2LocalData/index.tsx:294.

  - The only remaining auto-pick behavior is the plain “no database= and no dataset=” case, where it still falls back to databases[0].

  So for your specific question:

  - will it persist? Yes, if saved with the GF database selected, it should persist there.
  - will it save that it is under clickhouse? No. The dataset does not become “owned by ClickHouse.” ClickHouse is the serving backend, not the owner.
  - will it default to first database on the list? Not when the URL has a dataset that resolves. It only does that when there is no database= and no dataset= context.
  - will it persist with the correct one? Yes, that is the intended behavior now.

  When instances: [] actually happens:

  - A Database row can have many DHIS2Instance rows. See superset/dhis2/models.py:58.
  - instances: [] means there are no active DHIS2Instance rows for that database.
  - If there are no active instances, the code tries to migrate from legacy Database.encrypted_extra.
  - If encrypted_extra has no host or url, migration returns None, so you stay at instances: []. See superset/dhis2/instance_service.py:525.
  - That is the state we saw with Database 14: no legacy credentials to migrate, so no instance rows can be reconstructed.

  In short:

  - A new dataset under GF should stay under GF.
  - It should not become ClickHouse-owned.
  - It should not fall back to the first database if the dataset can be resolved.
  - instances: [] is a database-metadata problem, not a dataset-deletion problem,

### 2026-08-21 — slice-save validation and serving-wrapper hardening
- Root cause confirmed for the remaining **Failed to Save Slice** path:
  - background ETL syncs and the runtime metadata-validation repair could reset
    a `METADATA` wrapper's `schema` to `None` and move its database pointer
    from ClickHouse serving database ID `4` back to source DHIS2 database ID
    `5`;
  - on a chart save, Superset normalizes the chart payload, reads datasource
    columns, and reaches `SqlaTable.external_metadata()`. The old runtime
    repair therefore performed the bad reassignment immediately before the
    ClickHouse metadata probe.
- Backend fix (commit `88406f6`):
  - `superset/connectors/sqla/models.py` now keeps DHIS2 `METADATA` wrappers
    on their serving ClickHouse database with `schema="dhis2_serving"`;
  - the wrapper remains a friendly virtual dataset, but its SQL is normalized
    to a quoted ClickHouse serving-table reference, including specialized
    `_mart` tables;
  - `external_metadata()` consequently introspects the correct serving table
    during slice-save validation instead of querying the source DHIS2
    connection.
- ClickHouse exception code `47` was a separate chart-state symptom: legacy or
  duplicate charts referenced columns absent from their assigned dataset (for
  example pregnancy metrics issued against IRS Campaign dataset `8`).
- Dataset and state cleanup completed:
  - pregnancy dashboard charts were initially consolidated under
    `mal_preg_dataset` (dataset ID `360`); the current production
    registration is dataset ID `405` and is the authoritative target;
  - stale `params` / `query_context` JSON references were removed and Redis
    cache was flushed to eliminate dead-column references.
- Test coverage:
  - `TestDHIS2ServingMetadataWrapper` verifies runtime repair and
    `external_metadata()` against `sv_34_mal_preg_dataset_mart` inside a Flask
    application context;
  - the pytest-free production regression lives in
    `tests/unit_tests/dhis2/test_production_regressions_unittest.py`; the
    matching SQLA coverage is also present in
    `tests/unit_tests/connectors/sqla/models_test.py`;
  - run production-safe checks with:
```bash
cd /opt/dhis2-superset
sudo -u superset env HOME=/tmp bash -lc '
  set -a; . /etc/superset/superset.env; set +a
  /opt/superset-venv/bin/python scripts/run_unit_tests.py
'
```
  - the runner creates a Superset Flask app context and skips pytest-only test
    modules cleanly when `pytest` is not installed in the production venv.

### Operational guidance after the serving-wrapper fix
- **Overnight monitoring:** on Monday, confirm the active `mal_preg_dataset`
  (currently dataset ID `405`) remains on
  serving database ID `4` with `schema="dhis2_serving"`; it must not revert to
  `None`.
- **Specialized map plugins:** custom payload fields such as `org_unit` and
  `map_type` can still fail standard Superset REST API validation in the UI
  modal. Avoid UI **Save As** for these choropleth charts while that plugin
  contract remains outside the core slice schema; use a reviewed Python ORM
  script to update slice parameters instead.
- **New charts:** create them directly against the staged or MART dataset on
  **DHIS2 Serving (ClickHouse)**. Do not drag-and-drop swap datasources on
  older legacy charts, since their serialized payload can retain invalid
  columns from the previous dataset.

### 2026-08-24 — production repair confirmed and missing-staging hardening
- Production repair completed successfully:
  - active `mal_preg_dataset` confirmed as SqlaTable ID `405`, with
    `schema="dhis2_serving"`;
  - all 16 identified pregnancy charts were rebound to ID `405`;
  - nested `query_context.queries[*].datasource` keys were removed from 44
    DHIS2 charts, preventing `QueryObjectFactory.create()` from receiving the
    datasource twice;
  - Redis was flushed and `superset`, `superset-worker`, and `superset-beat`
    were restarted.
- This resolves the known chart-binding and duplicate-datasource failures.
  New ordinary charts should remain stable through the normalizer and guarded
  repair path. The next scheduled sync remains the production confirmation
  that the worker environment is running this code.
- Missing transient staging tables are now handled safely:
  - ClickHouse `table_exists()` uses `EXISTS TABLE` before a serving build;
  - a missing `dhis2_staging.ds_<id>_<name>` logs
    `Staging table ... does not exist in ClickHouse. Skipping build.` and
    returns the existing serving reference/registered columns instead of
    issuing a failing diagnostic query (Code `60` / `UNKNOWN_TABLE`);
  - `ensure_specialized_marts_for_sqla_table()` additionally catches a Code
    `60` build failure so `SqlaTable.fetch_metadata()` can continue using an
    existing serving table.
- Regression checks passed locally:
  - `pytest -q tests/unit_tests/dhis2/test_superset_dataset_service.py tests/dhis2/clickhouse_build_service_test.py tests/dhis2/test_staged_dataset_service.py`
  - `59 passed`.

### 2026-08-24 — ClickHouse-only binding and auto-sync safeguards
- DHIS2 serving dataset registration now resolves its serving `Database` by
  `Database.sqlalchemy_uri.ilike("%clickhouse%")`. A stale supplied database
  ID pointing to DuckDB, SQLite, or the Superset metadata database is not used
  for the serving datasource.
- Both physical serving datasets and friendly `METADATA` wrappers are locked
  to the ClickHouse database with `schema="dhis2_serving"`. DHIS2 source
  database details remain in `extra` for DHIS2 API and map routing only.
- Added `resolve_clickhouse_serving_table_name(table_name)`:
  - it queries `SHOW TABLES FROM dhis2_serving` for a friendly dataset name;
  - it resolves names such as `test_03_dataset` to their physical `sv_*` table;
  - when both exist, it prefers `sv_*_<name>_mart` over the base serving table.
- Added guarded metadata self-healing for DHIS2 staged-local datasets:
  - before a chart query, registered `TableColumn` names are compared with the
    staged dataset's serving-column registration;
  - if columns are missing, `SqlaTable.fetch_metadata()` refreshes the
    datasource before SQL compilation;
  - if ClickHouse still raises a column-resolution error (`UNKNOWN IDENTIFIER`,
    missing column, or Code `47`), metadata is refreshed and the query is
    recompiled and retried once before returning an error.
- Verification after this safeguard:
  - `pytest -q tests/unit_tests/dhis2/test_superset_dataset_service.py tests/dhis2/clickhouse_build_service_test.py tests/dhis2/test_staged_dataset_service.py`
  - `60 passed`;
  - targeted SQLA model checks: `2 passed`.

### 2026-08-24 — chart-save placeholder cleanup
- Explore chart saves could fail with HTTP `422 Unprocessable Content` and:
  `Chart contains unresolved DHIS2 references and cannot be saved: params:
  True, __metric__, query_context: True, __metric__`.
- Root cause: initial/default control state from custom chart plugins was being
  serialized into the save request as placeholder values, including
  `"__metric__"`, `"True": true`, and `params: true`. These are not real
  columns or metrics, but the strict DHIS2 payload normalizer correctly saw
  them as unresolved references.
- Fix applied before `ChartPostSchema` / `ChartPutSchema` validation for both
  `POST /api/v1/chart/` and `PUT /api/v1/chart/<id>`:
  - `params: true` is converted to an empty JSON object;
  - `__metric__` entries are removed from metric arrays and metric slots;
  - boolean placeholder entries such as `"True": true` are removed from
    `params`, direct `form_data`, `query_context.form_data`, and query objects.
- This keeps the existing strict guard for genuine missing DHIS2 columns while
  allowing charts saved with untouched/default plugin controls to persist.
- Verification:
  - new sanitizer regression test passed;
  - `pytest -q tests/unit_tests/dhis2/` completed with `42 passed`;
  - the requested `tests/unit_tests/charts/api_tests.py` path is absent in this
    checkout; the equivalent chart API tests are under
    `tests/integration_tests/charts/api_tests.py`.

### 2026-08-24 — unresolved-reference collector safeguard
- A second, collector-level safeguard was added to
  `collect_dhis2_chart_unresolved_refs()` so that placeholder control values
  cannot trigger `ChartDhis2UnresolvedReferencesValidationError` even if they
  bypass the API pre-save sanitizer.
- The collector now discards `__metric__`, `True` / `true`, `params`,
  `query_context`, and literal boolean `True` / `False` values before building
  invalid references and again before returning the unresolved result.
- As a result, a payload containing only default plugin placeholders now
  produces `unresolved == {}`. Real missing DHIS2 columns remain strict save
  blockers.
- Verification:
  - `pytest -q tests/unit_tests/dhis2/test_superset_dataset_service.py`
  - `23 passed`.

### 2026-08-24 — permanent serving-dataset persistence lock
- Automated Superset dataset cleanup now has a hard persistence lock for
  DHIS2-serving resources. Before any `SqlaTable` deletion/pruning operation,
  it retains the record if any of the following are true:
  - its schema is `dhis2_serving`;
  - it has bound chart slices;
  - its name contains `test`, `mart`, `preg`, or `sv_`.
- A blocked cleanup logs:
  `Persistence lock active: Blocked deletion of dataset id=%s name='%s'`.
- The guard is applied to orphaned-MART cleanup, legacy-MART cleanup, stale
  duplicate pruning, cross-database pruning, and staged-resource cleanup.
  This prevents nightly metadata/Celery cleanup from removing manually created
  test/custom serving datasets or the datasource rows that charts still use.
- Chart binding repair continues to update `params` and `query_context` in the
  same database transaction as `datasource_id`, `datasource_type`, and
  `datasource_name`; unresolved charts remain fail-closed and are not rebound.
- Serving database selection remains ClickHouse-only through the URI filter,
  with no automatic DuckDB/SQLite fallback for DHIS2 serving datasets.
- Verification:
  - `python -m py_compile superset/dhis2/superset_dataset_service.py` passed;
  - `pytest -q tests/unit_tests/dhis2/test_superset_dataset_service.py`
    completed with `23 passed`.

### 2026-08-25 — chart-engine metadata normalization and ClickHouse aggregation recovery
- Resolved the recurring ClickHouse `Code 184` (`ILLEGAL_AGGREGATION`) and
  `Code 215` (`NOT_AN_AGGREGATE`) failures affecting DHIS2 Explore and
  dashboard charts.
- Root cause was corrupted Superset `TableColumn` metadata, not ClickHouse
  table contents:
  - Dataset `277` (`dhis2_serving.sv_28_mal_prevention_mart`) had
    `ou_level.expression = SUM(\`ou_level\`)` and several raw numeric columns
    with `SUM(\`c_105_...\`)` expressions stored in PostgreSQL;
  - Superset substituted those expressions while compiling otherwise valid
    filters/metrics, producing invalid `WHERE SUM(ou_level) IN (...)` clauses
    and nested `SUM(SUM(...))` metrics;
  - direct ClickHouse verification using `WHERE ou_level IN (3)` succeeded,
    proving the physical MART and its data were healthy.
- A contributing chart regression had also injected virtual `period_variant`
  SQL, raw `period` grouping/order controls, and other over-normalized payload
  state. These generated an ungrouped raw-period `ORDER BY` and caused Code
  `215` on affected aggregate charts.
- Permanent behavior now uses physical serving columns:
  - `dataset_columns_payload()` keeps DHIS2 aggregation recommendations only
    as metadata hints; it no longer stores `SUM(...)` or bare column SQL in
    `TableColumn.expression`;
  - `period` and `period_variant` remain categorical, groupable/filterable
    physical columns (`is_dttm=False`, `expression=None`);
  - `ou_level` is preserved as a groupable/filterable hierarchy dimension;
  - chart payload normalization no longer forces period axes, grouping, or
    raw-period ordering; the saved/runtime OU filter guard remains in place.
- Recovery performed safely in PostgreSQL only:
  - `clear_dhis2_physical_column_expressions.py` cleared generated physical
    expressions from Dataset `277`; it intentionally retained the valid
    built-in `COUNT(*)` metric;
  - `inspect_dhis2_dataset_sql_expressions.py` confirmed
    `ou_level.expression=null`, `groupby=true`, no remaining physical column
    expressions, and a successful direct ClickHouse query;
  - chart payload cleanup removed stale duplicated period controls and invalid
    ordering without altering valid user-defined metrics or physical tables.
- Important diagnostic clarification: captured unsaved Explore requests sent
  raw `ou_level` filters. The aggregate was introduced by stale/corrupt
  datasource metadata (and temporarily cached datasource state), not by the
  browser request itself.
- Current state: dataset controls were refreshed, Redis/application state was
  flushed/restarted, and affected legacy charts—including `1st ANC and LLINs
  distribution`—now issue raw physical filters such as
  `WHERE ou_level IN (...)` without ClickHouse aggregation errors.
